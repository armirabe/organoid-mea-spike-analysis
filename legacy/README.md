# Legacy scripts

The original analysis scripts, preserved unmodified for provenance.

They are kept here so the published analysis remains inspectable. They are
not maintained, and `Consolidated_Neural_Data_Processing_Code.m` contains a
line of non-code at line 117 that prevents it running to completion. See
`../PORTING_NOTES.md` for the full list of known issues.

Use the functions in `../src/` for new work.
